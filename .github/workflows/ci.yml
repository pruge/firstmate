name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  # Docs-only fast path detection. `docs` matches every doc-surface path
  # (.agents/**, docs/**, root *.md); `other` matches everything else, so a
  # change touching bin/**, tests/**, scripts/**, .github/**, or any other
  # non-doc file sets it.
  # Downstream gates read exactly two facts: when detection did not succeed,
  # or `other` fired, every behavior lane runs; only a succeeded detection
  # with nothing outside the doc surfaces takes the fast path. Pushes to main
  # never detect (they follow an approved merge); verified-merge below decides
  # whether such a push skips these lanes or runs them in full.
  # That job narrows the push path: when the pushed head
  # commit matches either merge shape this repo produces (squash "(#<number>)"
  # or "Merge pull request #<number> from ..."), the same green PR already ran
  # these lanes, so they skip instead of re-running on an identical tree.
  # lint and invariants always run, even on a judged merge.
  # On code PRs each behavior lane additionally narrows itself to affected
  # tests through bin/fm-test-run.sh's conservative path->family map
  # (--changed --lane/--family): a lane whose diff maps to none of its tests
  # prints "skipped: no affected tests" and reports success in seconds.
  # ASSUMPTION: main has no branch protection (verified 2026-08-24), so skipped
  # jobs can never block a merge; revisit this fast path before adding required
  # status checks.
  changes:
    name: Detect docs-only change
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    outputs:
      other: ${{ steps.filter.outputs.other }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            docs:
              - '*.md'
              - 'docs/**'
              - '.agents/**'
            other:
              - '**'
              - '!*.md'
              - '!docs/**'
              - '!.agents/**'

  # Judged from the head commit message alone, because querying that PR's real
  # check results would need permissions beyond contents: read. Any commit this
  # pattern does not confidently recognize prints verified=false and falls back
  # to the full suite; skipping CI requires a confident match.
  # The job also runs (always judging false) on pull_request events: GitHub
  # Actions auto-skips every dependent of a skipped job even when that
  # dependent's own if is true, so letting this job be skipped on PRs would
  # silence every behavior lane there. Running it keeps its conclusion an
  # explicit false instead of a contagious skip.
  verified-merge:
    name: Detect verified PR merge
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    outputs:
      verified: ${{ steps.judge.outputs.verified }}
    steps:
      - uses: actions/checkout@v6
      - id: judge
        env:
          FM_HEAD_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
        run: |
          set -eu
          if [ "$GITHUB_EVENT_NAME" != "push" ]; then
            # Pull_request events have no head_commit to judge, and a lane skip
            # must never propagate downstream: judge false explicitly.
            echo "verified=false" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          bin/fm-ci-verified-merge.sh >>"$GITHUB_OUTPUT"

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install pinned ShellCheck
        run: |
          set -eu
          bin/fm-install-shellcheck.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install pinned actionlint
        run: |
          set -eu
          bin/fm-install-actionlint.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      # Single owner of the lint definition (shell file set, config, version,
      # and GitHub workflow lint). Do not re-spell the checks here; keep CI
      # and the pre-push gate on this script so a self-broken ci.yml still
      # fails locally before merge.
      - run: bin/fm-lint.sh

  # Docs-only PRs still owe their audience inventory proof: the portable serial
  # lane that normally carries tests/fm-documentation-audiences.test.sh skips,
  # so it runs standalone here. On code PRs this stays skipped and the shard
  # covers it.
  documentation-audiences:
    name: Documentation audiences test
    runs-on: ubuntu-latest
    needs: [changes]
    # Unchanged by verified-merge: on any push to main this condition is already
    # false (detection never succeeds there), so a judged merge needs no extra
    # clause here.
    if: needs.changes.result == 'success' && needs.changes.outputs.other != 'true'
    steps:
      - uses: actions/checkout@v6
      - run: tests/fm-documentation-audiences.test.sh

  # Deterministic proof that portable parallel shards + portable serial + Herdr
  # equal the complete tests/*.test.sh inventory with no missing or duplicates,
  # and that the portable serial CI shards partition that serial lane exactly.
  test-coverage:
    name: Test coverage guard
    runs-on: ubuntu-latest
    # Docs-only changes cannot add, remove, or move a test script, so the
    # partition proof cannot change its verdict.
    needs: [changes, verified-merge]
    if: >-
      !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true')
      && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true')
    steps:
      - uses: actions/checkout@v6
      - name: Prove complete regression partition
        run: bin/fm-test-run.sh --check-coverage

  # Two duration-balanced portable parallel shards of the Phase 2 proven-isolated
  # set only. Composition owner: bin/fm-test-run.sh (docs/fm-test-portable-shards.md).
  tests-portable-parallel-1:
    name: Behavior portable parallel 1
    runs-on: ubuntu-latest
    needs: [changes, verified-merge]
    if: >-
      !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true')
      && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true')
    # Measured shard wall is ~1 min of serial sum on proven scripts; this cap is
    # a hang tripwire with margin, not the expected healthy end of the lane.
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Detect affected tests for portable parallel shard 1
        id: affected
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -eu
          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            # Pushes to main reach these lanes only when not judged a verified
            # PR merge (an unrecognized shape): run the complete lane.
            echo "count=all" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          base=$(git merge-base "$PR_BASE_SHA" HEAD)
          count=$(bin/fm-test-run.sh --list --changed --base "$base" \
            --lane portable-parallel-1 | wc -l | tr -d ' ')
          echo "base=$base" >>"$GITHUB_OUTPUT"
          echo "count=$count" >>"$GITHUB_OUTPUT"
      - name: Report skip when nothing is affected
        if: steps.affected.outputs.count == '0'
        run: |
          echo "skipped: no affected tests"
      - name: Install pinned ShellCheck
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-shellcheck.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install pinned actionlint
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-actionlint.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install tasks-axi
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          npm install -g tasks-axi
          tasks-axi --version
      - name: Run portable parallel shard 1 (affected tests only)
        if: steps.affected.outputs.count != '0'
        env:
          FM_CHANGED_BASE: ${{ steps.affected.outputs.base }}
        run: |
          set -eu
          mkdir -p "$RUNNER_TEMP/fm-test"
          bin/fm-test-run.sh --lane portable-parallel-1 \
            --changed --base "$FM_CHANGED_BASE" \
            --json "$RUNNER_TEMP/fm-test/fm-test-timing-portable-parallel-1.json"
      - name: Upload shard 1 timing artifact
        if: always() && steps.affected.outputs.count != '0'
        uses: actions/upload-artifact@v4
        with:
          name: fm-test-timing-portable-parallel-1
          path: ${{ runner.temp }}/fm-test/fm-test-timing-portable-parallel-1.json
          if-no-files-found: warn

  tests-portable-parallel-2:
    name: Behavior portable parallel 2
    runs-on: ubuntu-latest
    needs: [changes, verified-merge]
    if: >-
      !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true')
      && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true')
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Detect affected tests for portable parallel shard 2
        id: affected
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -eu
          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            # Pushes to main reach these lanes only when not judged a verified
            # PR merge (an unrecognized shape): run the complete lane.
            echo "count=all" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          base=$(git merge-base "$PR_BASE_SHA" HEAD)
          count=$(bin/fm-test-run.sh --list --changed --base "$base" \
            --lane portable-parallel-2 | wc -l | tr -d ' ')
          echo "base=$base" >>"$GITHUB_OUTPUT"
          echo "count=$count" >>"$GITHUB_OUTPUT"
      - name: Report skip when nothing is affected
        if: steps.affected.outputs.count == '0'
        run: |
          echo "skipped: no affected tests"
      - name: Install pinned ShellCheck
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-shellcheck.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install pinned actionlint
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-actionlint.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install tasks-axi
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          npm install -g tasks-axi
          tasks-axi --version
      - name: Run portable parallel shard 2 (affected tests only)
        if: steps.affected.outputs.count != '0'
        env:
          FM_CHANGED_BASE: ${{ steps.affected.outputs.base }}
        run: |
          set -eu
          mkdir -p "$RUNNER_TEMP/fm-test"
          bin/fm-test-run.sh --lane portable-parallel-2 \
            --changed --base "$FM_CHANGED_BASE" \
            --json "$RUNNER_TEMP/fm-test/fm-test-timing-portable-parallel-2.json"
      - name: Upload shard 2 timing artifact
        if: always() && steps.affected.outputs.count != '0'
        uses: actions/upload-artifact@v4
        with:
          name: fm-test-timing-portable-parallel-2
          path: ${{ runner.temp }}/fm-test/fm-test-timing-portable-parallel-2.json
          if-no-files-found: warn

  # Required portable serial remainder: watcher, lock, AFK, tmux, daemon,
  # ambiguous, and other stateful tests. Real Herdr stays in tests-herdr.
  # Split across separate runners so no two of these stateful scripts ever share
  # a machine: each shard is still strictly serial in itself. Shard membership
  # and the shard count both belong to bin/fm-test-run.sh, which refuses a lane
  # whose "ofN" disagrees with it (docs/fm-test-portable-shards.md).
  tests-portable-serial:
    name: Behavior portable serial ${{ matrix.shard }}
    runs-on: ubuntu-latest
    # Measured whole remainder is ~50 min of serial work; the six balanced
    # shards are ~8.4 min each. Cap is a hang tripwire with roughly 2x margin,
    # not the expected healthy end of the lane.
    timeout-minutes: 15
    needs: [changes, verified-merge]
    if: >-
      !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true')
      && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true')
    strategy:
      # Every shard reports so one failure never hides another shard's result.
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6]
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Detect affected tests for this serial shard
        id: affected
        env:
          FM_SERIAL_LANE: portable-serial-${{ matrix.shard }}of${{ strategy.job-total }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -eu
          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            # Pushes to main reach this shard only when not judged a verified
            # PR merge (an unrecognized shape): run the complete shard.
            echo "count=all" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          base=$(git merge-base "$PR_BASE_SHA" HEAD)
          count=$(bin/fm-test-run.sh --list --changed --base "$base" \
            --lane "$FM_SERIAL_LANE" | wc -l | tr -d ' ')
          echo "base=$base" >>"$GITHUB_OUTPUT"
          echo "count=$count" >>"$GITHUB_OUTPUT"
      - name: Report skip when nothing is affected
        if: steps.affected.outputs.count == '0'
        run: |
          echo "skipped: no affected tests"
      - name: Install pinned ShellCheck
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-shellcheck.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install pinned actionlint
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-actionlint.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Require tmux for e2e tests
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          command -v tmux >/dev/null || {
            echo "::error::tmux is required for real afk injection e2e coverage"
            exit 1
          }
          tmux -V
      - name: Install tasks-axi for backlog-handoff delegation
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          npm install -g tasks-axi
          tasks-axi --version
      - name: Run portable serial shard ${{ matrix.shard }}
        # Exactly one automatic retry of this same shard: a passing rerun counts
        # as green and leaves a warning annotation as flake evidence. Never
        # retried more than once, and no other step retries. The shard runs
        # only its changed-selected intersection; the detect step above proved
        # it is non-empty.
        if: steps.affected.outputs.count != '0'
        env:
          # job-total rather than a literal, so shrinking or growing the matrix
          # without matching bin/fm-test-run.sh is refused instead of quietly
          # leaving a shard of the required lane unrun.
          FM_SERIAL_LANE: portable-serial-${{ matrix.shard }}of${{ strategy.job-total }}
          FM_SERIAL_SHARD: ${{ matrix.shard }}
          FM_CHANGED_BASE: ${{ steps.affected.outputs.base }}
        run: |
          set -eu
          mkdir -p "$RUNNER_TEMP/fm-test"
          json="$RUNNER_TEMP/fm-test/fm-test-timing-portable-serial-${FM_SERIAL_SHARD}.json"
          if bin/fm-test-run.sh --lane "$FM_SERIAL_LANE" \
              --changed --base "$FM_CHANGED_BASE" --json "$json"; then
            exit 0
          fi
          echo "::warning::portable serial shard ${FM_SERIAL_SHARD} failed; re-running the same shard once"
          if bin/fm-test-run.sh --lane "$FM_SERIAL_LANE" \
              --changed --base "$FM_CHANGED_BASE" --json "$json"; then
            echo "::warning::portable serial shard ${FM_SERIAL_SHARD} passed on retry; first failure kept as flake evidence"
            exit 0
          fi
          exit 1
      - name: Upload portable serial shard ${{ matrix.shard }} timing artifact
        if: always() && steps.affected.outputs.count != '0'
        uses: actions/upload-artifact@v4
        with:
          name: fm-test-timing-portable-serial-${{ matrix.shard }}
          path: ${{ runner.temp }}/fm-test/fm-test-timing-portable-serial-${{ matrix.shard }}.json
          if-no-files-found: warn

  # Required real-Herdr lane: pinned install, serial real-herdr-gated family,
  # hard-fail on "skip: herdr not found". Starts on Linux x86_64; if a genuine
  # platform invariant fails (focus, cleanup, default-session tripwire), keep
  # the failure evidence and move the job to macOS rather than skip assertions.
  tests-herdr:
    name: Behavior tests (Herdr)
    runs-on: ubuntu-latest
    needs: [changes, verified-merge]
    if: >-
      !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true')
      && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true')
    # Healthy runs finish around 7 minutes. This job cap is a last-resort hang
    # tripwire, not the expected end of the lane. The family-run step owns the
    # tighter bound so a wedged suite fails fast with always() cleanup and
    # timing artifacts still uploaded (docs/fm-test-portable-shards.md).
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Detect affected tests for the real-Herdr lane
        id: affected
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -eu
          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            # Pushes to main reach this lane only when not judged a verified
            # PR merge (an unrecognized shape): run the complete lane.
            echo "count=all" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          base=$(git merge-base "$PR_BASE_SHA" HEAD)
          count=$(bin/fm-test-run.sh --list --changed --base "$base" \
            --lane real-herdr-gated | wc -l | tr -d ' ')
          echo "base=$base" >>"$GITHUB_OUTPUT"
          echo "count=$count" >>"$GITHUB_OUTPUT"
      - name: Report skip when nothing is affected
        if: steps.affected.outputs.count == '0'
        run: |
          echo "skipped: no affected tests"
      - name: Require tools for real Herdr
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          command -v jq >/dev/null || { echo "::error::jq is required"; exit 1; }
          command -v python3 >/dev/null || { echo "::error::python3 is required"; exit 1; }
          command -v curl >/dev/null || { echo "::error::curl is required"; exit 1; }
          command -v tar >/dev/null || { echo "::error::tar is required"; exit 1; }
          jq --version
          python3 --version
      - name: Install pinned Herdr
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-install-herdr.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Install pinned Treehouse
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          # Several real-herdr-gated E2E scripts require treehouse for spawn
          # worktree acquisition (presentation, workspace-per-home, autodetect).
          bin/fm-install-treehouse.sh "$RUNNER_TEMP/bin"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
      - name: Assert Herdr pin and protocol floor
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          command -v herdr >/dev/null || { echo "::error::herdr not on PATH after install"; exit 1; }
          command -v treehouse >/dev/null || { echo "::error::treehouse not on PATH after install"; exit 1; }
          herdr --version
          treehouse --version
          status=$(herdr status --json)
          printf '%s\n' "$status"
          version=$(printf '%s' "$status" | jq -r '.client.version // empty')
          protocol=$(printf '%s' "$status" | jq -r '.client.protocol // empty')
          [ "$version" = "0.7.4" ] || {
            echo "::error::expected exact Herdr pin 0.7.4, got ${version:-<empty>}"
            exit 1
          }
          case "$protocol" in
            ''|*[!0-9]*) echo "::error::could not read client protocol"; exit 1 ;;
          esac
          [ "$protocol" -ge 16 ] || {
            echo "::error::herdr protocol $protocol is below the required floor 16"
            exit 1
          }
      - name: Start default Herdr session for fleet-state tripwire
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          # Lab provision requires exactly one running default session so the
          # default-session tripwire can arm. Start it headless; never use it
          # for lab work (tests still refuse default and use fm-lab-* only).
          mkdir -p "$RUNNER_TEMP/fm-herdr"
          nohup herdr server >"$RUNNER_TEMP/fm-herdr/default-server.log" 2>&1 &
          echo $! >"$RUNNER_TEMP/fm-herdr/default-server.pid"
          attempt=0
          while [ "$attempt" -lt 150 ]; do
            running=$(herdr status --json 2>/dev/null | jq -r '.server.running // false' || echo false)
            if [ "$running" = true ]; then
              herdr session list --json | jq -e '
                [.sessions[]? | select(.default == true and .name == "default" and .running == true)]
                | length == 1
              ' >/dev/null
              echo "default Herdr session is running"
              exit 0
            fi
            sleep 0.2
            attempt=$((attempt + 1))
          done
          echo "::error::default Herdr server did not become ready"
          cat "$RUNNER_TEMP/fm-herdr/default-server.log" || true
          exit 1
      - name: Snapshot pre-suite Herdr sessions
        if: steps.affected.outputs.count != '0'
        run: |
          set -eu
          mkdir -p "$RUNNER_TEMP/fm-herdr"
          bin/fm-herdr-ci-cleanup.sh snapshot "$RUNNER_TEMP/fm-herdr/sessions-before.json"
      - name: Run real-Herdr family (serial, required)
        # Comfortably above the ~7 min healthy wall and far below the 75 min
        # job backstop. A hang must fail this step so cleanup still runs.
        # Runs only its changed-selected intersection; the detect step above
        # proved it is non-empty.
        timeout-minutes: 20
        if: steps.affected.outputs.count != '0'
        env:
          FM_CHANGED_BASE: ${{ steps.affected.outputs.base }}
        run: |
          set -eu
          mkdir -p "$RUNNER_TEMP/fm-test"
          # Serial only. Fail if any script reports herdr not found. Live
          # harness credential tests stay outside this family (opt-in env only).
          bin/fm-test-run.sh --family real-herdr-gated \
            --changed --base "$FM_CHANGED_BASE" \
            --fail-on-gate-skip 'herdr not found' \
            --json "$RUNNER_TEMP/fm-test/fm-test-timing-herdr.json"
      - name: Cleanup job-owned Herdr lab sessions
        if: always() && steps.affected.outputs.count != '0'
        run: |
          set -eu
          bin/fm-herdr-ci-cleanup.sh teardown "$RUNNER_TEMP/fm-herdr/sessions-before.json" || {
            echo "::warning::lab session cleanup reported a problem; see logs"
            # Surface diagnostics without weakening the suite result above.
            herdr session list --json || true
            exit 1
          }
      - name: Upload Herdr timing and diagnostics
        if: always() && steps.affected.outputs.count != '0'
        uses: actions/upload-artifact@v4
        with:
          name: fm-test-timing-herdr
          path: |
            ${{ runner.temp }}/fm-test/fm-test-timing-herdr.json
            ${{ runner.temp }}/fm-herdr/sessions-before.json
            ${{ runner.temp }}/fm-herdr/default-server.log
          if-no-files-found: warn

  # Aggregate per-lane timing into one summary artifact for critical-path review.
  tests-timing-aggregate:
    name: Behavior timing aggregate
    runs-on: ubuntu-latest
    needs:
      - tests-portable-parallel-1
      - tests-portable-parallel-2
      - tests-portable-serial
      - tests-herdr
      - changes
      - verified-merge
    # Still aggregates when a lane failed (that is when the artifacts matter
    # most), but skips entirely on a docs-only PR or a judged verified PR merge
    # on main, where no lane ran.
    if: ${{ !cancelled() && !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true') && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true') }}
    steps:
      - uses: actions/checkout@v6
      - name: Download lane timing artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: fm-test-timing-*
          path: ${{ runner.temp }}/fm-test-aggregate
          merge-multiple: true
      - name: Build aggregate timing summary
        run: |
          set -eu
          mkdir -p "$RUNNER_TEMP/fm-test"
          shopt -s nullglob
          inputs=("$RUNNER_TEMP"/fm-test-aggregate/fm-test-timing-*.json)
          if [ "${#inputs[@]}" -eq 0 ]; then
            echo "::warning::no per-lane timing JSON found to aggregate"
            exit 0
          fi
          bin/fm-test-run.sh --aggregate-json \
            "$RUNNER_TEMP/fm-test/fm-test-timing-aggregate.json" \
            "${inputs[@]}"
      - name: Upload aggregate timing artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fm-test-timing-aggregate
          path: ${{ runner.temp }}/fm-test/fm-test-timing-aggregate.json
          if-no-files-found: warn

  macos-stock-bash:
    name: Stock macOS Bash snapshot compatibility
    runs-on: macos-latest
    timeout-minutes: 10
    needs: [changes, verified-merge]
    if: >-
      !(github.event_name == 'push' && needs.verified-merge.outputs.verified == 'true')
      && (needs.changes.result != 'success' || needs.changes.outputs.other == 'true')
    steps:
      - uses: actions/checkout@v6
        with:
          # Full history so the lane can compute its own merge-base.
          fetch-depth: 0
      - name: Detect affected tests for the macOS snapshot lane
        id: affected
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -eu
          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            # Pushes to main reach this lane only when not judged a verified
            # PR merge (an unrecognized shape): run the complete lane.
            echo "count=all" >>"$GITHUB_OUTPUT"
            exit 0
          fi
          base=$(git merge-base "$PR_BASE_SHA" HEAD)
          count=$(bin/fm-test-run.sh --list --changed --base "$base" \
            --family snapshot-bearings | wc -l | tr -d ' ')
          if [ "$count" -eq 0 ]; then
            # The stock-Bash parse sweep covers every tracked bin/*.sh, so any
            # shell-script change re-runs this lane even when no snapshot test
            # is affected.
            if git diff --name-only "$base...HEAD" | grep -qE '^bin/.*\.sh$'; then
              count=1
            fi
          fi
          echo "base=$base" >>"$GITHUB_OUTPUT"
          echo "count=$count" >>"$GITHUB_OUTPUT"
      - name: Report skip when nothing is affected
        if: steps.affected.outputs.count == '0'
        run: |
          echo "skipped: no affected tests"
      - name: Run snapshot consumers with stock Bash
        if: steps.affected.outputs.count != '0'
        shell: /bin/bash {0}
        env:
          PATH: /bin:/usr/bin:/usr/sbin:/sbin:/usr/local/bin:/opt/homebrew/bin
        run: |
          set -eu
          case "$BASH_VERSION" in
            3.2.57*) ;;
            *) echo "::error::expected stock macOS Bash 3.2.57, got $BASH_VERSION"; exit 1 ;;
          esac
          /bin/bash --version | head -1
          command -v jq >/dev/null || { echo "::error::jq is required"; exit 1; }

          shell_inventory="$RUNNER_TEMP/fm-shell-inventory"
          bin/fm-lint.sh --list-files > "$shell_inventory"
          parse_fail=0
          while IFS= read -r f; do
            /bin/bash -n "$f" || { echo "::error::stock macOS Bash 3.2 failed to parse $f"; parse_fail=1; }
          done < "$shell_inventory"
          [ "$parse_fail" -eq 0 ] || { echo "::error::stock macOS Bash 3.2 parse sweep failed"; exit 1; }

          snapshot_output=$(/bin/bash tests/fm-fleet-snapshot-view.test.sh)
          printf '%s\n' "$snapshot_output"
          snapshot_count=$(printf '%s\n' "$snapshot_output" | grep -c '^ok - ')
          [ "$snapshot_count" -eq 15 ] || {
            echo "::error::expected 15 snapshot/fleet-view tests, got $snapshot_count"
            exit 1
          }

          bearings_output=$(/bin/bash tests/fm-bearings-snapshot.test.sh)
          printf '%s\n' "$bearings_output"
          bearings_count=$(printf '%s\n' "$bearings_output" | grep -c '^ok - ')
          [ "$bearings_count" -eq 42 ] || {
            echo "::error::expected 42 Bearings tests, got $bearings_count"
            exit 1
          }

  invariants:
    name: Repo invariants
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Compatibility pointers must stay intact
        run: |
          set -eu
          [ ! -L CLAUDE.md ] || { echo "::error::CLAUDE.md must be a real @AGENTS.md pointer file, not a symlink"; exit 1; }
          tmp=$(mktemp)
          trap 'rm -f "$tmp"' EXIT
          printf '%s\n' \
            '<!-- Points Claude at AGENTS.md via import; edit AGENTS.md, not this file. -->' \
            '@AGENTS.md' >"$tmp"
          cmp -s CLAUDE.md "$tmp" || { echo "::error::CLAUDE.md must be the canonical @AGENTS.md pointer"; exit 1; }
          [ "$(readlink .claude/skills)" = "../.agents/skills" ] || { echo "::error::.claude/skills must be a symlink to ../.agents/skills"; exit 1; }
      - name: Personal fleet paths must not be tracked
        run: |
          set -eu
          tracked=$(git ls-files -- data state config projects .no-mistakes)
          if [ -n "$tracked" ]; then
            echo "::error::Personal fleet paths are tracked in git:"
            printf '%s\n' "$tracked"
            exit 1
          fi
