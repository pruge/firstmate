# T01 — 포트 페어: runtime-lib 이식 + fm-spawn 연결
Time: started=2026-08-26T20:38:12+09:00 finished=2026-08-24T23:02:45+09:00

## Goal
접수 시 배정된 포트 페어가 spawn에서 워크트리까지 전달되게 한다.

## Why this slice
"crew가 앱을 실행"의 첫 조각. 포트 충돌을 설계로 제거하는 계약의 본체.

## Scope
- bin/fm-worktree-runtime-lib.sh 이식·적응(현행 구조에 맞게)
- bin/fm-spawn.sh: --backend-port/--frontend-port 플래그, 검증, state/<id>.meta 기록, 라이브러리 호출 지점
- tests/fm-worktree-runtime-lib.test.sh + fm-spawn-ports-guard.test.sh 이식

## Implementation notes
- 구 홈 ai2/firstmate/bin·tests 를 원본으로 참조하되 현행 fm-spawn(herdr 등 백엔드, 플래그 파싱 위치)에 맞춰 적응.
- 계약 유지: firstmate만 배정자, 파일 존재로만 판정, 잘못된 값은 실패.
- shellcheck 통과.

## Acceptance criteria
spec 1·3·5·6번.

## Verification
이식 테스트 green + fm-lint + diff 리뷰.

## Depends on
- none

## Can run in parallel with
- T02 (다른 파일 집합)
